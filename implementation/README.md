## Instalação

Para instalar as dependências do projeto, execute:

```bash
pip install -r requirements.txt
```
## Rodando a aplicação Streamlit

Para iniciar a interface gráfica com Streamlit, execute:

```bash
python -m streamlit run UI/appUI.py
```
ou 
```bash
py -m streamlit run UI/appUI.py
```